# 云主机免费服务器自动延期程序环境变量配置
# 创建日期: 2026-02-17
# 更新日期: 2026-02-22
# 使用说明: 复制此文件为.env并填入真实值

# 邮件通知配置（可选）
# SMTP服务器配置，用于发送延期结果通知和验证失败通知
# 验证失败通知：延期提交5小时后验证，如果发现延期未生效，会发送紧急邮件
# QQ邮箱示例：SMTP_HOST=smtp.qq.com, SMTP_PORT=587, SMTP_PASSWORD使用授权码
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-email-password
NOTIFICATION_EMAIL=receive@example.com

# Webhook通知配置（可选）
# 支持企业微信、钉钉、飞书等Webhook地址
WEBHOOK_URL=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=your-key

# 日志级别
LOG_LEVEL=INFO

# 延期验证配置
# 延期提交后多少小时进行验证（默认5小时）
# 验证方法：再次提交延期，如果成功说明上次延期失败
VERIFICATION_DELAY_HOURS=5

# 邮件发送限制
# 每日最大邮件发送数量（默认10封，0表示不限制）
# 防止邮件发送过多被标记为垃圾邮件
MAX_DAILY_EMAILS=10
